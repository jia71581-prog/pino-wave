# 二维声波神经算子研究完整说明

**资料口径：2026-09-23。** 本文是公开版的阅读入口。正式论证、图注和文献见 [19 页论文](REVIEWER_PACKAGE_20260921/paper/main.pdf)及其 [LaTeX 源文件](REVIEWER_PACKAGE_20260921/paper/main.tex)；机器可读结果均在 [审稿资料包](REVIEWER_PACKAGE_20260921/)中。这里的“完整”指研究问题、方法、证据、负面结果和复现边界的完整交代，并不表示已公开数据和权重。

## 1. 研究问题与当前结论

目标是从二维速度场、震源信息与前 8 帧初始条件，**单次查询**输出 1 秒内的完整压力波场。它面向同一类物理问题的多次查询：先付训练成本，再用神经算子减少单次预测时间。网络按时间块直接查询，没有把前一预测帧反馈为下一帧输入；随时间增长的幅度与相位误差仍然可能发生。

目前最可靠的结论是：所评估的单次映射在复杂介质的晚期散射波上有明显精度缺口；本文提供误差定位、冻结校正方向的条件诊断和受限干预证据，**没有证明该缺口是所有神经算子的通用下界**。项目要求 validation 和独立 `test_id` 上总体及每介质族的相对 L2 都不高于 **0.05**，且包含适配在内的端到端平均与 P95 加速均至少 **10×**。目前 validation 精度门已失败，`test_id` 未开放，联合目标**未达成**。[验证摘要](REVIEWER_PACKAGE_20260921/results/validation_480_summary.json)和[最终门限记录](REVIEWER_PACKAGE_20260921/protocols/GATE_RESULT.json)提供原始口径。

## 2. 物理问题、数据和模型

| 项目 | 本研究的实际设置 |
|---|---|
| 方程 | 二维变速、常密度声波方程；顶部自由表面，另外三侧 CFS-CPML 吸收边界 |
| 真值求解 | LWC-84：时间四阶、空间八阶；401×401、5 m 网格，限制到 201×201、10 m 存储网格 |
| 输出 | 每条记录 401 帧，间隔 2.5 ms，覆盖 0–1 s 的压力场 |
| 输入 | 前 8 帧初始条件、速度模型和 Ricker 震源参数；震源主频覆盖约 8–30 Hz |
| 数据划分 | train 2800、validation 600、`test_id` 600；本文报告 uniform、layered、Marmousi 三族，validation 实际纳入 480 条 |

锚模型是 step **29359** 的 grouped UFNO：速度编码、attention、MIONet 融合、学习得到的低秩背景场和稠密波场解码器。检查点级参数数目为 **39,103,589**。代码把背景分支叫作 `coarse`，但它是**学习得到的压力场，不是粗网格数值求解**；模型前向没有 FD 传播循环。与验证谱对应的 **A4 radial-44 step 22814** 是另一训练谱系。9 月 23 日图表另用 `scno_homogeneous_longrun_gap15_v1` 的 update 5000 输出。这三组数字不可当成同一模型的连续测试。[代码快照](REVIEWER_PACKAGE_20260921/code/)、[配置](REVIEWER_PACKAGE_20260921/code/ic8_fullfield_40m_fullfam_e20_a4_radial44.yaml)、[论文 Setting](REVIEWER_PACKAGE_20260921/paper/main.tex)记录结构与协议。

## 3. 指标与证据分层

主指标对每条记录在完整未来窗口计算 `sqrt(sum((prediction − truth)^2) / sum(truth^2))`，以 float64 对全部网格点和帧累加；真值能量为零时结果未定义，并阻断聚合门。未来窗口从震源起始帧后的 8 帧初始条件结束处到最后一帧。B1/B2/B3 是该窗口的三个等长时间带。需要区分逐记录等权均值、逐族等权均值和能量池化 `sqrt(sum N / sum E)`；它们不可混写。本文的 **0.1** 是诊断阈值，并非 **0.05** 工程目标。

证据有三个范围：① 锚模型的 **160 条冻结 train 面板**用于误差机制诊断；② A4 模型的 **480 条 validation 记录**用于单独的谱与精度审计；③ 9 月 23 日的两条波场图与 9 条对照评分是 **train-dev** 监测证据。Marmousi 的 50 m 平移裁块还带来近邻相关风险，故即使 validation 分数也不应表述为完全独立介质泛化。`test_id` 保持封存。[冻结基线](REVIEWER_PACKAGE_20260921/protocols/BASELINE_FROZEN.json)、[排除组](REVIEWER_PACKAGE_20260921/protocols/EXCLUSION_GROUPS_FROZEN.json)与[论文局限性](REVIEWER_PACKAGE_20260921/paper/main.tex)给出细节。

## 4. 测得的精度与晚期误差

锚模型的冻结 **train** 面板采用逐记录等权相对 L2：

| 介质族 | 记录数 | 完整未来 | B1 早期 | B2 中期 | B3 晚期 |
|---|---:|---:|---:|---:|---:|
| uniform | 32 | 0.060902 | 0.054989 | 0.064430 | 1.745590* |
| layered | 32 | 0.230264 | 0.123030 | 0.398474 | 0.760080 |
| Marmousi | 96 | 0.396499 | 0.175279 | 0.359535 | 0.572774 |

`*` uniform 的晚期真值能量接近零，该分带相对误差分母病态，不能用它判断方法优劣。Marmousi 的 B3 则有充足信号：平均占每条记录未来真值能量 **30.61%**，却贡献 **64.10%** 的误差质量。逐记录恒等式和标量重算给出：即使把 B1/B2 误差设为零、保持 B3 原样，Marmousi 的完整未来误差仍为 **0.312137**（逐记录等权）或 **0.287059**（能量池化）。旧笔记中的 **0.3169** 来自把族均值相乘，不能作为精确聚合值。[标量源](REVIEWER_PACKAGE_20260921/results/diagnostic_audit_20260922/ENERGY_PANEL.json)与[重算脚本](REVIEWER_PACKAGE_20260921/results/diagnostic_audit_20260922/recompute_late_floor.py)可检查这一点。

A4 radial-44 的 **validation** 480 条记录，完整未来逐记录等权均值为 **0.352232**：uniform **0.130882**（90 条）、layered **0.302038**（240 条）、Marmousi **0.565352**（150 条）。同一批记录的能量池化总误差是 **0.475879**；不能拿它与前一列逐记录均值直接作效果比较。将所有谱带误差强行压到最低频带的现有误差、保持该频带不变，得到池化 **0.3641**，比 0.47588 低 23.5%。这是**有真值参与的条件反事实**，不是已实现的改善，更不是新架构的上限。[validation 摘要](REVIEWER_PACKAGE_20260921/results/validation_480_summary.json)与[分带标量](REVIEWER_PACKAGE_20260921/results/per_record_aggregates/error_spectrum_by_band.json)可复核。

## 5. 校正分支与干预：结论的适用范围

该模型的输出形式为学习背景场加上带尺度的学习校正。对**固定的**校正方向，允许逐帧用真值拟合最优标量后，在真实部署输出空间得到中位最优误差比 **0.999234**（12 条 train 记录、144 帧）。这表示单纯重缩放当前方向几乎无收益；它**不能**证明重新训练、改变特征或换模型也无收益。旧的 `0.9984` 探针漏计自由表面输出因子，不应再当部署空间界。[修正后的诊断数据](REVIEWER_PACKAGE_20260921/results/diagnostic_audit_20260922/CEILING_anchor29359.json)及[设计说明](REVIEWER_PACKAGE_20260921/results/diagnostic_audit_20260922/CEILING_DESIGN.md)明确了可比较范围。

预注册的 R4 反证臂从锚模型继续 **18,700** 步，Marmousi B3 从 **0.572774** 升到 **0.588998**，即相对变差 **2.83%**；uniform/layered 完整未来误差也变差。这个结果否定了该实现和预算下的晋级门，不能推出所有带宽、残差学习或物理引导方法无效。9 月 22 日的三组 100 步训练探索（时间覆盖、矩约束适配器、SCNO 启发残差网络）也没有通过各自的预注册改善门；本公开版仅在此记录其结论，不发布中间失败过程文件，也不把小样本短试验升级为泛化结论。论文与[修订记录](REVIEWER_PACKAGE_20260921/paper/REVISION_NOTES_20260922.md)区分了实测、诊断解释和未测假设。

## 6. 速度账与 9 月 23 日新增对照

在 RTX 4090 D 上，单条记录生成全部 401 帧的查询计时为：锚模型逐帧解码 **3.97 s**、16 帧分块 **2.54 s**；同一组三条 train 记录的传统 LWC-84 细网格 **20.07 s**、原生 201×201 网格 **10.00 s**。对应记录匹配的计时比为 **5.06×/7.90×**（对细网格）和 **2.52×/3.93×**（对原生网格）。16 帧分块只核过输出峰值，完整波场等价与精度尚未独立复查。计时排除磁盘 I/O、训练与取得初始条件的成本；输入信息也不同，因此这些是**条件查询计时比，不是精度匹配或端到端加速**。更便宜的原生网格求解器在三个族中位记录上均比锚模型更准。[冻结计时](REVIEWER_PACKAGE_20260921/results/runtime_20260922/)及[汇总表](REVIEWER_PACKAGE_20260921/results/paper_comparisons_20260923/group4_tables/TABLE_runtime.md)列出了协议。

9 月 23 日比较使用另一个 update 5000 神经模型，仅有两条 **train-dev** 波场记录：

| 记录 | 神经算子 | 51×51 粗解 | 101×101 粗解 |
|---|---:|---:|---:|
| train_marmousi_00076 | 0.279 | 0.514 | 0.120 |
| train_layered_00299 | 0.196 | 0.103 | 0.030 |

表内均为完整未来相对 L2。数值求解器要求奇数节点，因此用了 51×51 而非 50×50。Marmousi 的 51×51 粗解出现接收器相位滞后和频谱下移；神经模型主要呈现晚期振幅亏缺。但 layered 粗解明显更准，101×101 在两条记录上都更准。粗解误差还包含介质采样影响，这两条记录不代表族总体。[比较方法、数字和图](REVIEWER_PACKAGE_20260921/results/paper_comparisons_20260923/)均已收录。

同预算 DeepONet 基线在计划 **22,814** 步中的 **4,862** 步停止，固定 12 条 validation 批次的最佳稠密相对 L2 约 **1.000012**，接近零预测器水平。它没有完成 480 条 census 评测，因而不存在同口径的已收敛模型性能排名。该失败作为**最终对照判词**在[表格](REVIEWER_PACKAGE_20260921/results/paper_comparisons_20260923/group4_tables/TABLE_deeponet.md)中保留，而非公开中间失败运行文件。

## 7. 复核方法、公开范围与局限

先运行以下无需权重和 GPU 的核验：

```bash
cd REVIEWER_PACKAGE_20260921
sha256sum -c SHA256SUMS
python3 verify_package.py
python3 results/diagnostic_audit_20260922/recompute_late_floor.py
```

`SHA256SUMS` 检查本次发布内容；`verify_package.py` 核对 33 个代码文件的来源哈希、谱带恒等式和条件反事实；最后一条用公开的逐记录标量重算晚期量。论文可在安装 LaTeX 依赖后于 `paper/` 下执行 `latexmk -pdf main.tex`。完整模型预测和图件重生还需要原始 HDF5、对应模型权重、数值求解环境及脚本所指的输入路径。**本仓库没有这些大文件，因此只具备数值审计与代码来源复核能力，不具备独立重训能力。**

发布的 [代码来源表](REVIEWER_PACKAGE_20260921/code/PROVENANCE.tsv)对应原始审稿包内 33 个文件；[比较脚本](REVIEWER_PACKAGE_20260921/results/paper_comparisons_20260923/scripts/)对应 9 月 23 日最终图表。后一批图表使用的 SCNO 扩展训练快照与权重未装入这个审稿包，原脚本也有本机绝对路径；它们提供方法与数字审计，不能独立恢复该模型。未收录任何 HDF5、`.npy` 原始波场、权重、检查点、临时日志和中间失败运行目录。旧 `experiment-results/` 历史归档已从当前分支移除，先前 Git 提交仍保存历史。

接下来的可信推进需要先把 Marmousi 平移近邻按组隔离，再冻结新模型、训练预算与门限，最后做一次同协议 validation 和独立 `test_id` 审核；还需把初始条件获取、适配和 P95 耗时算入速度门。更密的时间监督、改变校正特征、传播子或求解器辅助方案目前都只是候选研究方向。本公开结果不赋予它们已验证的收益。
