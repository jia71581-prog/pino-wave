完成。三条验证门全过,无阻塞。

## 改动的文件

新建 3 项,**未修改任何既有文件**(`git status --porcelain` 对既有文件为空;16 项 v14 绑定在变更前与启动前、运行内部前后、以及最终复核共四次全部零漂移;spec sha256 保持 `ae977f2d…`):

| 文件 | sha256 |
|---|---|
| `scripts/probe_r4e8_masked_oracle.py` | `3373b583…` |
| `tests/saved_time_phase_operator_v4/test_r4e8_masked_oracle.py` | `408a540f…` |
| `results/r4e8_masked_oracle_probe_20260826/terminal.json` | `ca3b1c24…` |
| `results/r4e8_masked_oracle_probe_20260826/energy_profiles.json` | `3585b3d3…` |

产物 220 KB(上限 500 MB)。GPU 144.4 s,峰值 reserved 5.43 GiB,PID 40580,父检查点 `448035bd…` 前后一致、写入 0 次,disk 稳定 12 G。零训练、零反向、零检查点,只读 train。

## 三条验证门

**(a) 自一致性 — 通过,且是逐位精确**

| 量 | 记录 | 已发布 | 复现 | 差 |
|---|---|---|---|---|
| uniform rank16 逐帧未平方 reduction | `train_uniform_00002` (future_start 24) | −1.639005450056983 | −1.639005450056983 | **0.000e+00** |
| layered rank16 capture | `train_layered_01032` (future_start 14) | 0.6430370502360689 | 0.6430370502360689 | **0.000e+00** |

我预置的容差(1e-2 / 5e-3)完全没用上。附带两条更强的保真证据:重建基截断到 rank16 与冻结 `basis_rank16.pt` 的最小主余弦 ≥ 0.99980;我的口径 (i) 增益与已发布 estimator 在 3 族 × 3 rank 共 9 点上**全部一致到 1e-9 内**。

**(b) 新测试 — 30 项通过**,含近零真值帧那个 case:E=[4, 1, 1e-8, 9],err²=[1, 0.25, 4, 2.25],τ=1e-3 → floor=0.009。口径 (i) = 20001.5/4 = **5000.375**(单帧衰减就摧毁帧均值),口径 (ii) = **恰好 0.5**,口径 (iii) = √(7.5/14.00000001)。另断言掩码丢弃帧上的误差放大 10⁶ 倍不移动 (ii),以及掩码一致率可以失败(0.50 < 0.90)而不是只会通过。

**(c) `test_r16_dscp*.py` = 197 通过**(72+72+53,exit 0);与新文件合跑 227 通过。

## 结论(按预先声明的判定规则)

**掩码一致率 ≥ 0.90:通过。** uniform 0.9948、layered 0.9895、marmousi 1.0000。**父能量掩码是合法的部署因果代理**,掩码设计在 v15 冻结前无需更改。

**rank 是否瓶颈:三族都不满足"rank16 ≥ 0.20 且 rank32 增量 < 0.05"。** 规则第二分支生效 —— 按族报可达增益,**不设单一 0.5 阈值**,`uniform_single_threshold_permitted = False`。

口径 (ii) 每族可达增益(masked-matched 上界臂):

| 族 | 记录 | r8 | r16 | r32 | r32−r16 |
|---|---|---|---|---|---|
| uniform | 00321 | +0.077 | **+0.160** | +0.295 | +0.135 |
| layered | 00564 | +0.273 | **+0.386** | +0.573 | +0.187 |
| marmousi | 00385 | +0.060 | **+0.123** | +0.250 | +0.127 |

只有 layered 过 0.20;三族 rank32 增量都远超 0.05,所以**rank 仍是活跃瓶颈**,rank16 未饱和。

## 需要你裁决的一点(我没有自行扩大实验,但必须报)

规格第 4 条指定用现成 `weighted_coefficient_target` 路径,而该路径的权重 `w_t = 1/‖truth_t‖²` **恰好是口径 (i) 的目标函数**,因此它在口径 (ii) 下不是上界。实测它在 uniform/layered 上给出**负**增益(−0.149 / −0.227),capture 也为负(−0.35 / −0.67)。为了让规格开篇那个问题("口径 (ii) 下的 oracle 上界增益")真正可答,我加了一条明确标注的 `masked_matched` 臂(仅在保留帧上、用 floor 后分母做加权 LS),上表即该臂。两臂都用同一套冻结阈值 0.20/0.05/0.90 跑了判定规则,两臂**结论一致**(都落到第二分支),所以这一步没有改变任何判定。

顺带确认了 v14 常数的完整来源链:`0.2224565778108675 / 0.37334545199603053 / 0.29789271567937464` 就是 fresh3 结果里 rank16 加权臂的 `paired_changes_vs_parent.mean_per_frame_relative_l2_reduction`,分别来自 00102 / 01032 / 合成记录。同一口径在被评分记录上重算为 **0.2428 / 0.4228 / 0.1178**,与被搬运的常数无一相符,marmousi 差 2.5 倍 —— 该门确实不可判定。

uniform 的 `parent_metrics` 口径 (i) 达 **16.93**,而口径 (ii) 仅 **0.0909**:388 帧里 178 帧被掩码丢弃。这直接证实了晚期分母病态,也说明 v14 那个 22.09 vs 16.92 的比较本身就没有物理含义。
