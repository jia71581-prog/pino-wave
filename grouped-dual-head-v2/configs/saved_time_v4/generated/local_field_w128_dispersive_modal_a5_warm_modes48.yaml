# A5-WARM-MODES48 -- MODES ESCALATION of dispersive_modal_a5_warm. SINGLE FACTOR vs that config:
# local_field_dispersive_modal_modes 16 -> 48. gate_init 0.03, parent warp_r1, LR groups, panels,
# seed (372), max_frequency 8.0 all IDENTICAL to dispersive_modal_a5_warm.
#
# WHY THIS RUN EXISTS (2026-07-30 R8+16, per-pixel-frequency oracle on warp_r1 residual, late 160-400,
# scripts/residual_rank_oracle.py perpixel_freq_floor, PROVENANCE .../residual_rank_perpixel.json):
# the oracle proves A5's per-pixel-frequency form GENUINELY BEATS A3's shared rank at every matched
# budget (layered late, 3-record preview):
#     M     shared_rank(A3)  perpixel_freq(A5)  ratio
#      8     0.269            0.188              0.70
#     16     0.177            0.101              0.57   <- A5 default modes=16 floors at 0.10 on layered late
#     32     0.092            0.066              0.72
#     64     0.046            0.040              0.85
# So the layered late residual has REAL local temporal-frequency structure (multi-arrival/dispersion) ->
# A5 is not falsified. BUT modes=16 is UNDER-PROVISIONED: its optimistic per-pixel floor 0.101 > the 0.08
# family target; per-pixel only crosses <0.05 near M~64. modes=48 is the conservative single escalation
# (per-pixel floor ~0.05 optimistic); if 48 lands in the 0.05-0.08 band a modes=64 follow-up is the next
# single factor. HONEST EXPECTATION: a learned generalizing basis will NOT beat the per-pixel SVD/DFT
# optimum, so modes=48 realistically lands layered late ~0.06-0.09; agg<0.05 plausible only because early
# frames are near-perfect under warp_r1. If modes=48 late stays >0.08 the dispersive-modal additive family
# is ceiling-bound and the true path is a structurally-different parent (not more modes).
#
# GATED LAUNCH: run ONLY if (a) a GPU frees, AND (b) the full-family oracle confirms per-pixel<shared on
# marmousi+uniform too (not just layered), AND (c) dispersive_modal_a5_warm (modes=16) has produced a
# strict verdict showing the gate opens + monotone improvement but tops out >0.08 on layered late (i.e.
# modes is the binding knob). If modes=16 already reaches <0.08 family -> no escalation. If it stalls at
# the warp ceiling (~0.235, gate never opens) -> fix training first, do NOT escalate modes.
#
# MEMORY: modes 16->48 grows only the dispersive_modal bank (A_m/omega_m/phi_m per-pixel conv maps),
# ~3x 62,305 = ~187k params, tiny vs the 5.58M base; frame-chunked cos-combine keeps count=401 from the
# (R,T,M,H,W) blowup. Peak still gated by maximum_peak_cuda_gib 23.5 + 2-epoch pilot. Smoke (>=3
# val->train boundaries, 4-rank no OOM/NaN) before the full run.
artifact_dir: /root/autodl-tmp/home/jiayh/Data/FNO-Acoustic-Wave-Simulation/1/pretraining/local_field_w128_hicap/dispersive_modal_a5_warm_modes48
base_config: configs/grouped_v3/continuous_pilot_w128_legacy_norm.yaml
checkpoint_keep_last: 5
checkpoint_transfer:
  allow_new_local_field_parameters: true
  allow_parent_manifest_mismatch: false
  parent_optimizer_state: false
pin_trainable_prefixes: [dense_decoder, local_field, source_encoder, fusion]
energy_floor_fraction: 0.01
epochs: 40
family_gradient_weights: {layered: 1.0, marmousi: 1.0, uniform: 1.0}
gate:
  external_evidence_gate: false
  family_regression_tolerance: 0.05
  maximum_peak_cuda_gib: 23.5
  pilot_epochs: 2
  target_aggregate_relative_l2: 0.05
  target_family_relative_l2: 0.08
loss:
  delta: 0.5
  delta_energy_floor_fraction: 0.1
  hard_causality: true
  hard_causality_lead_cycles: 1.0
  spatial_gradient: 0.1
  spectrum: 0.1
  temporal_difference: 0.1
  per_frame_frame: true
  frame_energy_floor_fraction: 0.05
  late_frame_gain: 2.0
  late_frame_start_fraction: 0.4
macro_records: 8
macros_per_update: 4
microbatch_records: 1
optimizer:
  adamw_implementation: fused
  backbone_learning_rate: 5.0e-7
  dense_learning_rate: 5.0e-6
  dispersive_modal_learning_rate: 5.0e-5
  full_forward_checkpointing: false
  geometry_learning_rate: 1.0e-6
  gradient_clip: 1.0
  gradient_clip_mode: prefix_limits
  gradient_clip_prefix_limits:
    coordinate_encoder: 5.0
    default: 1.0
    dense_decoder: 20.0
    fusion: 5.0
    local_field: 80.0
    medium_encoder: 2.0
    source_encoder: 5.0
    travel_branch: 5.0
  minimum_factor: 0.1
  schedule_epoch_offset: 0
  schedule_total_epochs: 40
  training_time_block: 1
  warmup_epochs: 2
  weight_decay: 1.0e-6
parent_checkpoint: /root/autodl-tmp/home/jiayh/Data/FNO-Acoustic-Wave-Simulation/1/pretraining/local_field_w128_hicap/warp_r1/run/best.pt
parent_checkpoint_identity: /root/autodl-tmp/home/jiayh/Data/FNO-Acoustic-Wave-Simulation/1/pretraining/local_field_w128_hicap/warp_r1/run/run_identity.json
parent_identity: /root/autodl-tmp/home/jiayh/Data/FNO-Acoustic-Wave-Simulation/1/pretraining/local_field_w128_residual/w2_parent_identity_legacy_norm.json
prefetch_factor: 4
query_points: 1
seed: 372
time_appearance_offset: 15
time_policy: appearance16
training_frames_per_record: 24
travel_time_h5: /home/jiayh/Data/data/processed/hybrid_travel_layered_eikonal_ray12_v1.h5
validation:
  all_records_every: 4
  final_frames_per_record: 401
  frames_per_record: 32
  full_panel_records: 48
  microbatch_records: 1
  panel_records: 48
variant_overrides:
  local_field: true
  local_field_residual: true
  local_field_warp: true
  local_field_warp_max_shift_cells: 8.0
  local_field_dispersive_modal: true
  local_field_dispersive_modal_modes: 48
  local_field_dispersive_modal_max_frequency: 8.0
  local_field_adapter_gate_init: 0.03
adaptive_sampling:
  enabled: true
  frame_axis: true
  record_axis: true
  k: 1.0
  c: 1.0
  floor_per_bin: 2
  record_oversample: 1.3
  time_bin_errors: [0.143, 0.260, 0.430]
  family_errors: {uniform: 0.183, layered: 0.301, marmousi: 0.325}
workers: 8
