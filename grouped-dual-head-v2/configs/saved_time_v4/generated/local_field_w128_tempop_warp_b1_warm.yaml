# B1-WARM: deadlock-broken WARP + temporal-operator stack.  SINGLE FACTOR vs tempop_warp_b1
# (the stalled B1 run): ONLY local_field_adapter_gate_init changes 0.0 -> 0.03.  parent (warp_r1
# best.pt), operator rank (8), spatial_kernel (3), warp, LR groups (warp 5e-5, temporal_operator
# 5e-5, parent near-frozen dense 5e-6), panels, seed (372) -- all IDENTICAL to tempop_warp_b1.
#
# WHY THIS RUN EXISTS (2026-07-30 R8+11, weight-level PROOF): tempop_warp_b1 plateaued at ep8
# agg=0.2355 / phase=0.8007 -- IDENTICAL to the separable A3 (0.234) and opt16 (0.2449).  Its ep8
# checkpoint gate = local_field.temporal_operator.gate = -8.96e-05: the multiplicative operator
# gate NEVER OPENED.  ROOT CAUSE (fixed this resume): _ContinuousTimePropagationBasis was
# instantiated WITHOUT a gate_init argument -- it hardcoded self.gate = zeros(()), so the config
# knob local_field_adapter_gate_init could not reach it and B1's deadlock was STRUCTURALLY
# unbreakable.  local_field.py now threads gate_init into the operator (default 0.0 = byte-identical;
# 696 v4 tests green).  CPU deadlock tests pin it: gate_init=0.0 -> time_trunk/spatial_coefficient
# grads == 0 (starved); gate_init=0.03 -> both get finite nonzero gradient at step 0.
#
# WHY B1-WARM MATTERS (goal-relevance beyond A3): the R8+9 residual oracle shows the SEPARABLE
# shared-temporal-basis corrector (A3 rank32/64) tops out at an optimistic ~0.10 agg (layered 0.141
# at rank32), short of the <0.05 hard goal.  The temporal OPERATOR is NON-separable (a per-(record,
# frame) spatial-neighbourhood corrector reading the rendered features, RF = 2*3-1 = 5), so it is
# not bound by that separable floor -- the more promising path below 0.235 toward the hard goal.
# Only with the gate open (this run) is that testable; the stalled B1 tested nothing.
#
# FALSIFICATION (pre-registered; ref = strict warp ep12: agg 0.2398, layered 0.2878, late tbin
# 0.3452, phase 0.7969).  By the ep12 strict panel require: aggregate < 0.235 AND layered
# family_relative_l2 < 0.27 AND late tbin < 0.30 while phase_correlation >= 0.80.  If, WITH the gate
# open and the operator receiving real gradient, layered/late stay pinned at the warp ceiling ->
# the rank-8 kernel-3 operator FORM is insufficient (not merely deadlocked) -> escalate to the
# dynamic Green/scattering kernel (green_r5) or a larger receptive field.  Unchanged hard goal: agg
# < 0.05 and every family < 0.08.
#
# LAUNCH: on a FREE GPU only; DISTINCT artifact_dir (..._warm) -- does NOT touch tempop_warp_b1 or
# the running A3 (PID 495074).  Preferred order relative to rank32_warm: rank32_warm first (cleanest
# single-factor deadlock test, reuses running A3 as gate0 control); B1-warm is the parallel/next
# non-separable arm and the stronger candidate for actually breaking below the separable ~0.10 floor.
artifact_dir: /root/autodl-tmp/home/jiayh/Data/FNO-Acoustic-Wave-Simulation/1/pretraining/local_field_w128_hicap/tempop_warp_b1_warm
base_config: configs/grouped_v3/continuous_pilot_w128_legacy_norm.yaml
checkpoint_keep_last: 5
checkpoint_transfer:
  allow_new_local_field_parameters: false
  allow_parent_manifest_mismatch: false
  parent_optimizer_state: false
pin_trainable_prefixes: [dense_decoder, local_field, source_encoder, fusion]
energy_floor_fraction: 0.01
epochs: 40
family_gradient_weights: {layered: 1.0, marmousi: 1.0, uniform: 1.0}
gate:
  external_evidence_gate: false
  family_regression_tolerance: 0.05
  maximum_peak_cuda_gib: 23.5
  pilot_epochs: 2
  target_aggregate_relative_l2: 0.05
  target_family_relative_l2: 0.08
loss:
  delta: 0.5
  delta_energy_floor_fraction: 0.1
  hard_causality: true
  hard_causality_lead_cycles: 1.0
  spatial_gradient: 0.1
  spectrum: 0.1
  temporal_difference: 0.1
  per_frame_frame: true
  frame_energy_floor_fraction: 0.05
  late_frame_gain: 2.0
  late_frame_start_fraction: 0.4
macro_records: 8
macros_per_update: 4
microbatch_records: 1
optimizer:
  adamw_implementation: fused
  backbone_learning_rate: 5.0e-7
  dense_learning_rate: 5.0e-6
  warp_learning_rate: 5.0e-5
  temporal_operator_learning_rate: 5.0e-5
  full_forward_checkpointing: false
  geometry_learning_rate: 1.0e-6
  gradient_clip: 1.0
  gradient_clip_mode: prefix_limits
  gradient_clip_prefix_limits:
    coordinate_encoder: 5.0
    default: 1.0
    dense_decoder: 20.0
    fusion: 5.0
    local_field: 80.0
    medium_encoder: 2.0
    source_encoder: 5.0
    travel_branch: 5.0
  minimum_factor: 0.1
  schedule_epoch_offset: 0
  schedule_total_epochs: 40
  training_time_block: 1
  warmup_epochs: 2
  weight_decay: 1.0e-6
parent_checkpoint: /root/autodl-tmp/home/jiayh/Data/FNO-Acoustic-Wave-Simulation/1/pretraining/local_field_w128_hicap/warp_r1/run/best.pt
parent_checkpoint_identity: /root/autodl-tmp/home/jiayh/Data/FNO-Acoustic-Wave-Simulation/1/pretraining/local_field_w128_hicap/warp_r1/run/run_identity.json
parent_identity: /root/autodl-tmp/home/jiayh/Data/FNO-Acoustic-Wave-Simulation/1/pretraining/local_field_w128_residual/w2_parent_identity_legacy_norm.json
prefetch_factor: 4
query_points: 1
seed: 372
time_appearance_offset: 15
time_policy: appearance16
training_frames_per_record: 24
travel_time_h5: /home/jiayh/Data/data/processed/hybrid_travel_layered_eikonal_ray12_v1.h5
validation:
  all_records_every: 4
  final_frames_per_record: 401
  frames_per_record: 32
  full_panel_records: 48
  microbatch_records: 1
  panel_records: 48
variant_overrides:
  local_field: true
  local_field_residual: true
  local_field_warp: true
  local_field_warp_max_shift_cells: 8.0
  local_field_temporal_operator_rank: 8
  local_field_temporal_operator_spatial_kernel: 3
  local_field_adapter_gate_init: 0.03
adaptive_sampling:
  enabled: true
  frame_axis: true
  record_axis: true
  k: 1.0
  c: 1.0
  floor_per_bin: 2
  record_oversample: 1.3
  time_bin_errors: [0.143, 0.260, 0.430]
  family_errors: {uniform: 0.183, layered: 0.301, marmousi: 0.325}
workers: 8
